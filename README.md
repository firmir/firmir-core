# Зависимости
```
sudo apt update
sudo apt install pkg-config libevent-dev
```

# Сборка
```
mkdir build
cd build
cmake ..
cmake --build . --config Release
make
```

# Запуск 
./event_loop
./bin/event_loop
